# Copy to .env and set real values. .env is gitignored (never committed).
# Used by docker-compose.yml for the web UI's HTTP Basic auth.
# Leave both empty to disable authentication.
WEB_USER=admin
WEB_PASSWORD=change-me
